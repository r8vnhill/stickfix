version = 1

# Use the actual test layout; DeepSource treats these as collected tests.
test_patterns = ["tests/**"]

[[analyzers]]
name = "python"
enabled = true

  [analyzers.meta]
  # Match the project's target interpreter (see pyproject.toml).
  runtime_version = "3.14.0"
