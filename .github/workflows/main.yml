name: GitHub Secrets
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - shell: pwsh
        env: 
          TEST_TOKEN: ${{ secrets.TEST_TOKEN }}
        run: |
          Write-Output "$env:TEST_TOKEN"
